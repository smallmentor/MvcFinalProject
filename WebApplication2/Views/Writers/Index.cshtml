@model IEnumerable<WebApplication2.Models.Writer>

@{
    ViewBag.Title = "作者";
}

<h2>作者</h2>

<p>
    @Html.ActionLink("新增", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WriterName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WriterName)
        </td>
        <td>
            @Html.ActionLink("修改", "Edit", new { id=item.WriterID }) |
            @Html.ActionLink("細節", "Details", new { id=item.WriterID }) |
            @Html.ActionLink("刪除", "Delete", new { id=item.WriterID })
        </td>
    </tr>
}

</table>
